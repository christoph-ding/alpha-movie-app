const keys = {
  // this is just a stub for github, to show where the key would go
  // of course i would never just push it to github ...
  'movie': 'this is not an actual key' 
};

module.exports = keys;
